How To Run This Program:

1. Install packages:
    pip install configparser
    pip install python-csv
    pip install PyQt6
    pip install openai==0.28

2. Paste the API key in ‘config.ini’ before executing the program.

3. Execute the command: "python3 main.py"
